# 2026-02-08

## Data Management Orbi — Project Status

### Architecture
- **Backend:** Node/Express/TS + better-sqlite3 + JWT + bcrypt → port 3001
- **Frontend:** React/Vite/TS + Tailwind + lucide-react → port 5173
- **DB:** SQLite (orbi.db) — plan to migrate to SQL Server later

### Directories
- `D:\DataManagementOrbi` — build/dev workspace (servers run here)
- `D:\Projetos\DataManagementOrbi` — git repo for GitHub push
- Files robocopy'd from build → repo (39 files)

### What's Done
- Full app working: login, dashboard, report viewer (iframe), admin panel (users/reports/permissions)
- Auth: JWT 8h, roles admin/user. Admin: alberto.junior / 332769
- DB seed: 1 admin + 5 fake users, 4 placeholder reports
- Both frontend and backend compile clean
- Git repo initialized, remote set to https://github.com/juniortesla53-cpu/DataManagementOrbi.git

### Still TODO
1. Clean up repo dir (delete stale node_modules if any)
2. Add .gitignore
3. npm install in repo's backend + frontend
4. git add, commit, push to GitHub
5. Get real Power BI embed links from Alberto
6. Get SQL Server query for user data (Matricula, LoginRede, NomeCompleto, Cargo, Cpf, Site)
7. Eventually: SQL Server integration, enterprise deploy

### Backend .env
- PORT=3001, JWT_SECRET=orbi-secret-change-me-in-production, DB_PATH=./orbi.db

### Notes
- better-sqlite3 chosen over sql.js (sync API, works fine with build tools)
- Robocopy exit code 1 = success on Windows
- Use `cmd /c` for shell commands (PowerShell environment)
